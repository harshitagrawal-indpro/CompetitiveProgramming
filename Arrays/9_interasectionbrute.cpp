// to intersect two arrays using brute force method 
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n , m ;
    int arr1[n], arr2[m];
    cin >> n >> m ;
    for (int i=0 ; i<n ;i++){
        cin >> arr1[i];
    }
    for (int i=0; i<m; i++){
        cin >> arr2[i];
    }

    vector <int> temp(5,0);
    
}
